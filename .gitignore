node_modules/
dist/
.wrangler/
# Local secrets. Wrangler reads `.dev.vars` and, for a named environment,
# `.dev.vars.<env>` — the Hono example uses named environments. The example
# templates are committed on purpose.
.dev.vars*
!.dev.vars.example
!.dev.vars.*.example
.env
.env.*
!.env.example
!.env.*.example
*.log
.DS_Store
.eslintcache
*.tsbuildinfo
coverage/

# Local tooling config stays out of git; set up per machine
.agents/
.claude/
.worktrees/
AGENTS.md
CLAUDE.md
.cursor/
.cursorrules
.windsurf/
.windsurfrules
.mcp.json
