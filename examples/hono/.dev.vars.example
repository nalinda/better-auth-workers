# Copy to .dev.vars (gitignored) for `wrangler dev`. Deployed Workers get
# this from `wrangler secret put BETTER_AUTH_SECRET` instead.
BETTER_AUTH_SECRET=replace-with-32-or-more-random-bytes-base64-encoded
